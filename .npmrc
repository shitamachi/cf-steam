# steamapi 是 git 依赖；npm 11+ 默认禁用 git 依赖（EALLOWGIT）。
# npm 11 的 allow-git 接受 boolean；npm 12 要求 all/none/root。
# 两行同 key 时后者覆盖前者：npm 11 读 true（合法），npm 12 读 true
# （warn invalid 但行为上仍允许 git 依赖），倒序则 npm 11 会用到 all（非法）。
allow-git=all
allow-git=true