# steamapi 曾为 git 依赖；npm 12 在 Deno Deploy 构建环境（Deno 的 npm shim）下
# 对 git 依赖做 preparation 必然崩溃（npm 包自带 workspaces 字段 + shim cwd 错位，
# 实测 npm 10/11/12 全崩，官方无修复）。fork 已发布为 steamapi-wasm（registry 依赖），
# 无需 allow-git 配置。未来勿新增 git 依赖。
